mixin button(text=null, type='', icon=null)
  button(class=`button-small button-tablet-medium button-desktop-large ${type}`)
    if icon
      img(class="button-icon" src=`../assets/icons/${icon}.svg` alt=`${icon}`)
    if text
      span(class="button-text") #{text}